# ALIASES
alias ls='ls --color'
alias la='ls -A'
alias pog='nvim $(fzf-tmux)'

# git
alias gap="git add -p"
alias gc="git commit"
alias gcm="git commit -m"
alias gp="git push"
alias gpu="git pull"
alias gck="git checkout"


alias x="nvim x.md"
alias con="tmux attach-session -t"
alias poker="pokemon-colorscripts -r"
alias poke="pokemon-colorscripts"
alias fetch="neofetch  --ascii_distro  windows7"

# the numebers are the 256 color codes
# refrence here --> https://www.ditig.com/256-colors-cheat-sheet
PROMPT='%F{167};%f %B%F{240}%1~ %f%b' 


export PATH
